name: Release Production apk
on:
  push:
    branches:
      - master
      - dev
  pull_request:
    branches:
      - master
      - dev

jobs:
  build:
    name: Build & Deploy a release APK
    env:
      APP_NAME: "Smartlets"
      TAG_PREFIX: "prod-release-"
      SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
    runs-on: ubuntu-latest
    steps:
      - name: Fetch Repo HEAD
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Bump version and push Release tag
        id: bump_tag
        uses: rymndhng/release-on-push-action@master
        with:
          bump_version_scheme: minor

      - name: Check Output Parameters
        run: |
          echo "Got tag name ${{ steps.bump_tag.outputs.tag_name }}"
          echo "Got release version ${{ steps.bump_tag.outputs.version }}"
