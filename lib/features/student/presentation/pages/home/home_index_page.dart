import 'package:flutter/material.dart';

class StudentHomeIndexPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: SafeArea(child: Text('Some stuff to be added to home')),
      ),
    );
  }
}
